specVersion: "v1"
serviceId: "musicfix-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "musicfix-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
